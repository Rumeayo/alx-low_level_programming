#include "main.h"

/**
 * add - this function sum up two numbers.
 * @i: arguement 1 that is passed when the function add is called.
 * @l: arguement 2 that is passed when the function add is called.
 * Return: returns the total sum.
 */
int add (int i, int l)
{
	int n;
	n = i + l;
	return (n);
}
